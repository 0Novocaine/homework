# Quotes

## Usage

`Install dependencies:`
$ poetry install
`Run`
* docker compose up -d
* poetry run python fpq/manage.py migrate
* poetry run python fpq/manage.py createsuperuser
* poetry run python fpq/manage.py runserver

Go to http://localhost:8000.
